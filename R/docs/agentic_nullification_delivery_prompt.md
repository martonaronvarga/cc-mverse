# Agentic Delivery Prompt: Empirical CSE Nullification

You are continuing the TDK2025 CSE multiverse audit. The central scientific goal is not to simulate a simplified RT world. The goal is to construct empirical counterfactual RT datasets from `R/all_indexed.csv` in which the target CSE (`cong:prev_cong`) is stripped at the raw-data stage while the non-CSE geometry of the collected time-series-like RT data is preserved as far as possible.

Primary invariants:

1. Use `R/all_indexed.csv`, generated by `R/mverse_indexed.Rmd`, as the primary data source because it preserves richer study, participant, trial, block, correctness, and row-order semantics than reduced merged CSVs.
2. Strip CSE before participant subsampling, outlier filtering, RT transformation, and model fitting. This order is intentional: it represents the philosophical event where the collected raw data had population CSE = 0, and any later apparent effect is induced by analytical procedures.
3. Remove `null_both` from branch generation, Rust effect conditions, R config/analysis labels, docs, and tests. It is not needed because it removes non-CSE effects and violates the counterfactual target.
4. Treat `additive_qmap` as the primary stationary participant-level CSE stripper only after preservation diagnostics pass. Treat `shuffle` as a secondary robustness check, not as the primary counterfactual.
5. Preserve marginal and geometric structure explicitly: participant identity, study labels, row order, trial/block indices, current- and previous-congruency marginal effects, cell counts, missingness policy, correctness/post-error structure where available, RT distribution shape, and temporal autocorrelation/trends.
6. Use canonical previous-trial fields computed study-wise from `all_indexed.csv`; do not trust leaked or stale `prev_cong`/`prev_correct` fields without auditing. Instead of recomputing, modify the data source as I have the original datafiles for each study. Feel free to tinker with `mverse_indexed.Rmd`
7. Report nullification-based FPR only with matching residual-CSE and preservation diagnostics. Known-truth simulation may be used as calibration, not as the primary source of realism.

Implementation plan:

1. Data contract: route Rust processing from `R/all_indexed.csv` or an explicit canonical preprocessed derivative that retains `study`, `trial_index`, `block_index`, `correct`, `prev_correct`, `source_row` and other relevant variables; reject reduced `merged_data.csv` as the primary path. Fix `previous_trial` and `prev_correct` semantics in the data processing, ensure the integrity and correctness of `all_indexed.csv`. The source of the data processing is `mverse_indexed.Rmd`.
2. Branch space: delete `null_both`; valid effect conditions are `present` and `null_interaction`, with `null_interaction` using explicit strip methods.
3. Primary stripper: keep qmap row/column marginal curves unshrunk in additive reconstruction; possibly add non-additive reconstruction or reconsider the method altogether; add diagnostics/tests for preservation of current/previous marginal effects, participant means, row order, and RT quantile geometry. Rename additive_qmap accordingly.
4. Alternative stripping: Add model-based local residual stripping: estimate nuisance time/block/post-error/trend structure, remove only the `cong:prev_cong` component, and recompose residuals in original row order.
5. Time-series support: implement block/trial-bin qmap where cells are adequate, with documented fallback to stationary qmap; add preservation diagnostics for lag autocorrelation, trial-index slopes, block means, post-error slowing, and transition/cell geometry.
6. Diagnostics: extend `nullification_diagnostics.csv` with pre/post residual CSE, quantile CSE, study, participant count, row count, missing/nonpositive RT, canonical previous-field mismatch, cell-count minima, row-order preservation, marginal-effect preservation, autocorrelation deltas, trial/block trend deltas, trial-index slopes, block means, post-error slowing, quantile CSE by time bin and warnings. Debrief 2026-05-26: diagnostics now include paired deltas from the matching `present` branch for mean/quantile CSE, marginal effects, lag-1 autocorrelation, trial slope, block mean spread, transition imbalance, max time-bin median quantile CSE, and post-error slowing; stationary `additive_qmap` currently fails preservation gates on the bounded all-indexed smoke subset.
7. Analysis outputs: rename empirical nullified branch rates to nullification-based FPR; include unconditional, conditional, and failure-aware denominators; stratify by strip method and preservation-diagnostic pass/fail.

Acceptance checks:

- Narrow Rust tests pass for qmap location/shape stripping and marginal preservation.
- Branch-generation tests prove `null_both` is absent.
- A local smoke command reads `R/all_indexed.csv` or its canonical derivative and writes processed branches retaining row-order semantics.
- Diagnostic smoke outputs show residual CSE near zero and preservation metrics for at least one small study/participant subset.
- Final docs state why stripping happens before all analytical transformations and why simulation is only calibration.
